using Microsoft.AspNetCore.Mvc;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace Onion.WebApp.Controllers
{
    public class DocumentController : Controller
    {
        //public FileResult GetDocument(int id)
        //{
        //    //string filePath = _Docs.GetById(id).Name;
        //    byte[] fileContent = _Docs.GetById(id).File;

        //    var currentFileType = _Docs.GetById(id).FileType;

        //    if (currentFileType != null)
        //    {
        //        return File(fileContent, currentFileType);
        //    }
        //    else
        //    {
        //        return File(fileContent, System.Net.Mime.MediaTypeNames.Application.Octet);
        //    }

        //}
    }
}
